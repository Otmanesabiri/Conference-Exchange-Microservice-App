spring:
  application:
    name: gateway-service-test
  cloud:
    config:
      enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/conference-realm

management:
  endpoints:
    web:
      exposure:
        include: health
