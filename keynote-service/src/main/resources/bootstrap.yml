spring:
  application:
    name: keynote-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 5
        initial-interval: 2000
        multiplier: 1.5

management:
  endpoints:
    web:
      exposure:
        include: health,info
